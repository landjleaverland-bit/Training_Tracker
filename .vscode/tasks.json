{
  "version": "2.0.0",
  "tasks": [
    {
        "label": "npm build",
        "type": "shell",
        "group": "build",
        "command": "npm run build",
        "options": {
            "cwd": "${workspaceFolder}"
        }
    },
    {
        "label": "npm start",
        "type": "shell",
        "group": "build",
        "command": "npm start",
        "options": {
            "cwd": "${workspaceFolder}"
        }
    }
]
}